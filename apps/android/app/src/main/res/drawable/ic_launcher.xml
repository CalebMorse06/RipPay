<!-- Launcher icon for pre-API-26 devices: white tile + centered RP mark bitmap. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/bg_primary" />
    <item
        android:drawable="@drawable/brand_mark"
        android:gravity="center"
        android:width="72dp"
        android:height="72dp" />
</layer-list>
