{
  "name": "coldtap",
  "private": true,
  "workspaces": [
    "apps/ios",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "start": "npm run start -w apps/web",
    "lint": "npm run lint -w apps/web",
    "typecheck": "tsc -p apps/web --noEmit && tsc -p packages/shared --noEmit"
  }
}
